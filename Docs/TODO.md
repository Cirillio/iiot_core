Что добавить в методичку:

- **Используй пагинацию**, если устройств станет больше 50.
- **Добавь индекс** в БД на колонку `is_active`, чтобы фильтрация работала мгновенно:  
    `CREATE INDEX ix_devices_is_active ON devices (is_active);`